I have a blog generation system in my nfdgames website repo that I need to reorganize. The generation infrastructure should live in my private OpenDrop repo, and only the output files (generated HTML, CSS) should stay in nfdgames.

## Current state (nfdgames repo)
The branch `claude/seo-blog-generation-plan-UzqoB` in nfantinodyer/nfdgames has:
- `blog/generate_blogs.py` — LLM-powered blog generation script (Anthropic Claude / OpenAI)
- `blog/topics.json` — 110 SEO topic definitions
- `blog/post-template.html` — HTML template for blog posts
- `blog/posts/` — output directory for generated HTML
- `styles/blog.css` — blog stylesheet
- `.gitignore` — protects .env files
- Nav links updated in `index.html` and `OpenDrop/index.html` to include Blog

## What I want

### Move TO the private OpenDrop repo (nfantinodyer/OpenDrop):
- `blog/generate_blogs.py`
- `blog/topics.json`
- `blog/post-template.html`

Put these in a `seo/` or `blog-generator/` directory inside OpenDrop. Update the script so that:
1. It outputs generated HTML files directly to the nfdgames repo's `blog/posts/` directory
2. It updates the nfdgames repo's `sitemap.xml` and `blog/index.html`
3. The output paths are configurable but default to `~/Documents/Github/nfdgames/`
4. The script path reference uses `~/Documents/Github/OpenDrop/` as its own base

### Keep IN nfdgames repo:
- `styles/blog.css` (the stylesheet)
- `blog/posts/` directory (where generated HTML goes)
- `blog/index.html` (auto-generated by the script)
- The nav link updates to `index.html` and `OpenDrop/index.html`
- `.gitignore`

### Remove FROM nfdgames repo:
- `blog/generate_blogs.py`
- `blog/topics.json`
- `blog/post-template.html`
(These will now live in the private OpenDrop repo only)

### Write a HOW-TO file in the OpenDrop repo explaining:
- How to install dependencies (`pip install anthropic`)
- How to set the API key (`export ANTHROPIC_API_KEY="..."`)
- All the commands: generate all, generate batch, generate single, dry-run, index-only
- That generated files go to the nfdgames repo and need to be committed/pushed there separately

### My local paths (macOS):
- nfdgames repo: ~/Documents/Github/nfdgames
- OpenDrop repo: ~/Documents/Github/OpenDrop

Please make these changes across both repos, commit, and push.
